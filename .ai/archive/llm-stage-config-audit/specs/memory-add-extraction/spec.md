# Spec Delta: memory-add-extraction

## REMOVED Requirements

### Requirement: Extraction Stage Contract

The system no longer maintains the legacy fact-extraction helper contract as part of current behavior.

### Requirement: Extraction Stage Control Surface

The system no longer exposes a dedicated legacy extraction stage override as a maintained runtime surface.

### Requirement: Extraction Prompt Guidance

The system no longer maintains the legacy fact-extraction prompt as part of the current supported `Memory.add()` pipeline.
